

# modo escritura
# "w": Modo de escritura. Abre el fichero para escribir. Si el fichero no existe, se crea. Si el fichero ya existe, se trunca (se borra su contenido).
fichero = open("ejemplo.txt", "w")

Escribir = fichero.write(" porque te vas chorromplina ")

fichero.close()





